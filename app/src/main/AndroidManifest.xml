<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thehorecamall">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.TheHorecaMall">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.TheHorecaMall.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.seller_log_in.SellerLoginActivity"/>
        <activity android:name=".ui.seller_log_in.SellerRegisterActivity"/>
        <activity android:name=".ui.login.SendCodeActivity"/>
        <activity android:name=".ui.login.PasteCodeActivity"/>
        <activity android:name=".ui.seller_log_in.SellerRegisterActivity_Page2"/>
        <activity android:name=".ui.seller_log_in.SellerCompletePageActivity"/>
        <activity android:name=".ui.detegrents.DetergentsActivity"/>
        <activity android:name=".ui.chat.ChatActivity"/>
        <activity android:name=".ui.address_choose.ChooseAddressActivity" android:windowSoftInputMode="adjustNothing"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>